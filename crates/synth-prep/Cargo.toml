[package]
name = "synth-prep"
version = "0.1.0"
edition = "2024"

[dependencies]
arrow-array = "59"
bytemuck = "1"
hf-hub = { version = "1.0.0-rc.1", features = ["blocking", "rustls-tls"] }
llama2-tokenizer = { path = "../llama2-tokenizer" }
parquet = "59"
